###############################################################################
# Minty-I3 Picom Configuration
###############################################################################

# Shadow
shadow = true;
shadow-radius = 8;
shadow-offset-x = -8;
shadow-offset-y = -8;
shadow-opacity = 0.5;
shadow-red = 0.0;
shadow-green = 0.0;
shadow-blue = 0.0;

# Fading
fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;
fade-delta = 10;

# Window opacity
inactive-opacity = 0.9;
active-opacity = 1.0;
frame-opacity = 0.9;
inactive-opacity-override = false;

# Blur
blur-background = true;
blur-background-frame = true;
blur-background-fixed = true;
blur-kern = "3x3box";
blur-strength = 8;

# Rounded corners
corner-radius = 0;
rounded-corners-exclude = [
  "window_type = 'dock'",
  "window_type = 'utility'"
];

# Backend
backend = "glx";
vsync = true;

# GLX settings
glx-no-stencil = true;
glx-no-rebind-pixmap = true;
xrender-sync-fence = true;
use-damage = true;

# Window type settings
wintypes:
{
  tooltip = { fade = true; shadow = true; opacity = 0.9; focus = true; };
  dock = { shadow = false; }
  dnd = { shadow = false; }
  popup_menu = { opacity = 1.0; }
  dropdown_menu = { opacity = 1.0; }
};

# Exclude conditions
shadow-exclude = [
  "name = 'Notification'",
  "window_type = 'dock'",
  "window_type = 'utility'",
  "_NET_WM_WINDOW_TYPE:a = 'dialog'",
  "_GTK_FRAME_EXTENTS@:c"
];

fade-exclude = [
  "window_type = 'dock'",
  "window_type = 'utility'"
];

# Transparency rules
opacity-rule = [
  "90:class_i = 'URxvt'",
  "90:class_i = 'XTerm'",
  "90:class_i = 'Terminator'"
];
